import React from 'react';
import CustomFooter from '@site/src/components/CustomFooter';

// This is a wrapper around our custom footer to maintain compatibility
const FooterWrapper = () => {
  return <CustomFooter />;
};

export default FooterWrapper;